# Clawdbot 模型切换指南

## 🎯 配置更新

已成功将 Clawdbot 默认模型从 `deepseek/deepseek-coder` 切换到 `deepseek/deepseek-reasoner`。

## 📋 当前模型配置

### 主模型
- **`deepseek/deepseek-reasoner`** - 默认使用

### 回退模型（按顺序）
1. `deepseek/deepseek-coder` - 原主模型
2. `deepseek/deepseek-chat` - DeepSeek 聊天模型
3. `minimax/MiniMax-M2.1` - MiniMax 模型
4. `qwen-portal/vision-model` - 通义千问视觉模型

### 可用 DeepSeek 模型
1. **`deepseek-reasoner`** - 推理优化模型（新添加）
2. `deepseek-coder` - 代码优化模型
3. `deepseek-chat` - 通用聊天模型

## 🔧 模型特性

### DeepSeek Reasoner
- **推理能力**: 强大的逻辑推理和问题解决
- **代码生成**: 高质量的代码实现
- **技术设计**: 优秀的技术架构设计
- **中文支持**: 良好的中文理解和生成

### DeepSeek Coder
- **代码专注**: 专门优化的代码生成
- **技术细节**: 详细的技术实现
- **效率优先**: 快速的代码响应

### 模型切换
```bash
# 在会话中使用 /model 命令切换模型
/model deepseek-reasoner
/model deepseek-coder
/model minimax
/model qwen
```

## 🚀 使用建议

### 推荐场景
1. **技术设计** - 使用 `deepseek-reasoner`
2. **代码实现** - 使用 `deepseek-coder`
3. **问题解决** - 使用 `deepseek-reasoner`
4. **文档生成** - 使用 `deepseek-reasoner`

### 性能提示
1. **推理任务**: Reasoner 模型表现更佳
2. **代码任务**: Coder 模型更专注
3. **复杂问题**: 优先使用 Reasoner
4. **简单任务**: 可以使用 Coder 提高响应速度

## 📊 配置验证

### 检查当前配置
```bash
clawdbot status
```

### 查看会话模型
```bash
# 查看当前会话使用的模型
clawdbot status --sessions
```

### 测试模型切换
```bash
# 测试不同模型的响应
/model deepseek-reasoner
请帮我设计一个系统架构

/model deepseek-coder
请帮我实现这个函数
```

## 🔄 回退机制

如果 `deepseek-reasoner` 不可用或出现问题，系统会自动按顺序尝试：
1. `deepseek/deepseek-coder`
2. `deepseek/deepseek-chat`
3. `minimax/MiniMax-M2.1`
4. `qwen-portal/vision-model`

## 📞 问题排查

### 常见问题
1. **模型不可用**: 检查 API 密钥和网络连接
2. **响应缓慢**: 尝试切换到其他模型
3. **质量不佳**: 调整提示词或切换模型

### 恢复默认
```bash
# 如果需要恢复原来的配置
cp ~/.clawdbot/clawdbot.json.backup ~/.clawdbot/clawdbot.json
```

## 🎉 开始使用

现在可以开始使用 DeepSeek Reasoner 模型了！

```bash
# 开始新的会话，将自动使用 deepseek-reasoner
你好，请帮我分析这个技术问题
```

**祝您使用愉快！** 🚀
